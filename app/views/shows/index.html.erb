<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FASC 2025 - Shows</title>
</head>
<body>
    <div class="container">
        <h1>🎶 Programação de Shows 🎶</h1>

        <% if @shows.any? %>
            <div class="grid">
                <% @shows.each do |show| %>
                    <div class="card">
                        <img src="<%= show['imgUrl'] %>" alt="<%= show['name'] %>">
                        <h2><%= show['name'] %></h2>
                        <p><%= show['description'] %></p>
                        <p class="info">📅 <%= show['startDate'].to_date %> | 📍 <%= show['location'] %></p>
                    </div>
                <% end %>
            </div>
        <% else %>
            <p class="text-center">Nenhum show encontrado.</p>
        <% end %>
    </div>
</body>
</html>
